export interface InputUpdateUserDTO {
    id: string;
    name: string;
    email: string;
    password: string;
}

export interface OutputUpdateUserDTO {
    id: string;
    name: string;
    email: string;
}