import exp from "constants";
import { User } from "../entity/User";

describe("User service unit tests", () => {
  it("should ", () => {
    
    //const user1 = new User("1", "John", "", "", new Date(), new Date());
    expect(true).toBe(true);
  });
});
